<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-create-orders-table" author="developer">
        <createTable tableName="order" schemaName="store">
            <column name="id" type="SERIAL" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="comment" type="TEXT">
                <constraints nullable="true"/>
            </column>
            <column name="status" type="VARCHAR(20)" defaultValue="pending">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="total_amount" type="DECIMAL(10,2)">
                <constraints nullable="false"/>
            </column>
            <column name="shipping_address" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="order_number" type="VARCHAR(100)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="payment_method" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="payment_status" type="VARCHAR(20)" defaultValue="pending">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Создание внешнего ключа -->
        <addForeignKeyConstraint
            baseTableName="order"
            baseColumnNames="user_id"
            baseTableSchemaName="store"
            constraintName="fk_order_user"
            referencedTableName="user"
            referencedColumnNames="id"
            referencedTableSchemaName="store"
            onDelete="CASCADE"/>

        <!-- Создание индексов -->
        <createIndex indexName="idx_order_user_id" tableName="order" schemaName="store">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_order_status" tableName="order" schemaName="store">
            <column name="status"/>
        </createIndex>

        <createIndex indexName="idx_order_number" tableName="order" schemaName="store">
            <column name="order_number"/>
        </createIndex>

        <createIndex indexName="idx_order_created_at" tableName="order" schemaName="store">
            <column name="created_at"/>
        </createIndex>

        <!-- Добавление ограничений -->
        <sql>
            ALTER TABLE store.order ADD CONSTRAINT check_order_status
            CHECK (status IN ('pending', 'confirmed', 'shipped', 'delivered', 'cancelled'));

            ALTER TABLE store.order ADD CONSTRAINT check_payment_status
            CHECK (payment_status IN ('pending', 'paid', 'failed', 'refunded'));

            ALTER TABLE store.order ADD CONSTRAINT check_total_amount
            CHECK (total_amount >= 0);
        </sql>
    </changeSet>

</databaseChangeLog>
