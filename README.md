# NestJS E-commerce API

Современное API для интернет-магазина, построенное на NestJS с использованием TypeORM и PostgreSQL.

## Особенности

- 🏗️ **Модульная архитектура** - User, Auth, Product, Order модули
- 🔐 **JWT аутентификация** с ролевой системой (USER, ADMIN)
- 🐘 **PostgreSQL** с TypeORM
- 🐳 **Docker** контейнеризация
- 📊 **Liquibase** для управления миграциями БД
- 🔧 **pgAdmin** для управления базой данных

## Быстрый старт

### 1. Клонирование и установка

```bash
git clone <repository-url>
cd project-back-reboot
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и настройте переменные:

```bash
cp .env.example .env
```

### 3. Запуск с Docker

```bash
# Запуск всех сервисов
docker-compose up --build

# Запуск только PostgreSQL и pgAdmin
docker-compose up postgres pgadmin
```

## Управление миграциями БД

### Применение миграций

**Windows:**
```cmd
migrate.bat update
```

**Linux/Mac:**
```bash
chmod +x migrate.sh
./migrate.sh update
```

### Другие команды миграций

```bash
# Проверка статуса миграций
migrate.bat status

# Валидация changelog файлов
migrate.bat validate

# История миграций
migrate.bat history

# Откат к определенному тегу
migrate.bat rollback <tag>
```

## Структура базы данных

Проект использует схему `store` с следующими таблицами:

- **user** - Пользователи системы
- **product** - Товары
- **order** - Заказы
- **product_order** - Связь товаров и заказов

Все миграции находятся в папке `db/changelog/migrations/`.

## API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход

### Пользователи
- `GET /users` - Список пользователей (ADMIN)
- `GET /users/profile` - Профиль текущего пользователя

### Товары
- `GET /products` - Список товаров
- `POST /products` - Создание товара (ADMIN)
- `PUT /products/:id` - Обновление товара (ADMIN)
- `DELETE /products/:id` - Удаление товара (ADMIN)

### Заказы
- `GET /orders` - Список заказов пользователя
- `POST /orders` - Создание заказа
- `GET /orders/:id` - Детали заказа

## Доступ к сервисам

- **API**: http://localhost:3000
- **pgAdmin**: http://localhost:8080 (admin@admin.com / admin)
- **PostgreSQL**: localhost:5432

## Разработка

### Локальный запуск

```bash
# Запуск только БД
docker-compose up postgres

# Запуск приложения в dev режиме
npm run start:dev
```

### Тестирование

```bash
# Unit тесты
npm run test

# E2E тесты
npm run test:e2e

# Покрытие тестами
npm run test:cov
```

## Технологический стек

- **Backend**: NestJS, TypeScript
- **База данных**: PostgreSQL 18
- **ORM**: TypeORM
- **Миграции**: Liquibase
- **Аутентификация**: JWT, bcryptjs
- **Контейнеризация**: Docker, Docker Compose
- **Администрирование БД**: pgAdmin 4

## Лицензия

[MIT licensed](LICENSE).
